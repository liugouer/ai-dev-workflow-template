---
description: 
globs:
alwaysApply: true
---
# SecurityRules

## 文件上传安全规则（如项目涉及文件上传）

1. 必须校验文件是否为空。
2. 必须校验文件扩展名（白名单机制）。
3. 必须限制文件大小。
4. 不允许执行上传文件内容。
5. 不允许信任用户传入的文件名。
6. 错误响应中不允许暴露服务器路径。
7. 不允许返回异常堆栈给前端。

## API 安全规则

1. 所有异常必须被捕获并转换为业务错误。
2. 禁止把密钥、Token、账号密码写入代码。
3. 禁止把真实云服务密钥提交到 Git。
4. 禁止在日志中输出敏感信息。

## 外部服务调用安全规则

1. 外部服务失败时必须返回明确错误。
2. 外部服务地址必须通过环境变量配置。
3. 不允许在代码中硬编码真实生产密钥。
4. Mock 返回值必须清晰标注为模拟结果。
